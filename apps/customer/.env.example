# Points the app at your locally-running apps/api, or the deployed one.
EXPO_PUBLIC_API_URL=http://localhost:8080

# Stripe TEST-mode publishable key (safe to expose client-side - starts
# with pk_test_...). Never put a secret key (sk_...) here.
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
